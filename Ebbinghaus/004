 - String Match Algorithm

- BCsma 
	charTable init;
	jump = 0;
	while jump < n-m, do // iterate text
		index = m-1; // each time, from tail of pattern to head
		
		while P[index] == T[index + jump], do
			index--;

		if (index < 0) return jump;

		jump += |jump - charTable[Text[index+jump]]|

	
